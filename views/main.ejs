<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
<style>
    p {
        font-size: 14px !important;
    }
</style>
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@3.4.1/dist/css/bootstrap.min.css">
<div class="col-md-4">
    <div class="jumbotron">
        <h2>El Comercio</h2>
        <div class="row">
        <% comercio.forEach(function(post) { %>
            <div class="col-md-6">
                <div class="thumbnail">
                    <h4><%= post[0] %></h4>
                    <a href="<%= post[1] %>">Leer Más</a>
                </div>
            </div>
        <% }); %>
        </div>
    </div>
</div>

<div class="col-md-4">
    <div class="jumbotron">
        <h2>Gestión</h2>
        <div class="row">
        <% gestion.forEach(function(post) { %>
            <div class="col-md-6">
                <div class="thumbnail">
                    <h4><%= post[0] %></h4>
                    <p><%= post[1] %></p>
                    <a href="<%= post[2] %>">Leer Más</a>
                </div>
            </div>
        <% }); %>
        </div>
    </div>
</div>
<div class="col-md-4">
<div class="jumbotron">
        <h2>Republica</h2>
        <div class="row">
        <% republica.forEach(function(post) { %>
            <div class="col-md-6">
                <div class="thumbnail">
                    <h4><%= post[0] %></h4>
                    <p><%= post[1] %></p>
                    <a href="<%= post[2] %>">Leer Más</a>
                </div>
            </div>
        <% }); %>
        </div>
    </div>
</div>
</body>
</html>
</div>